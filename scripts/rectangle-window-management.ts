// Name: Rectangle Window Management
// Shortcut: cmd shift d

import "@johnlindquist/kit"
import {UI} from "../../../../.kit/core/enum";

let keyInfo = await hotkey({

})
await hide();

await notify("Rectangle Window Management" )

